# append the path to .gitignore
echo "config/config.env" >> .gitignore

# also ensure .env is ignored (already present in your .gitignore, but safe to add)
echo ".env" >> .gitignore

# stage the updated .gitignore
git add .gitignore
git commit -m "Ignore local env files"
